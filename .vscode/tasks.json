{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Make install package",
            "type": "shell",
            "command": "pwsh -f Install.ps1",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}